% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dicepro.R
\name{plot_hyperopt}
\alias{plot_hyperopt}
\alias{plot_hyperopt.dicepro}
\title{Plot hyperparameter optimization report}
\usage{
plot_hyperopt(x, ...)

\method{plot_hyperopt}{dicepro}(
  x,
  params,
  metric = "loss",
  loss_metric = "loss",
  loss_behaviour = "min",
  not_log = NULL,
  categorical = NULL,
  max_deviation = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{dicepro} object. Trials are read from
\code{x$trials}.}

\item{...}{Currently unused. Reserved for future extensions.}

\item{params}{Character vector of hyperparameter column names to
display (e.g. \code{c("lambda_", "gamma", "p_prime")}).}

\item{metric}{Character scalar. Column used for point size
(default \code{"loss"}).}

\item{loss_metric}{Character scalar. Column used as the loss axis
(default \code{"loss"}).}

\item{loss_behaviour}{Character scalar. Direction of the loss:
\code{"min"} (default) or \code{"max"}.}

\item{not_log}{Character vector of parameter names that should
not be log-scaled on their axis (default \code{NULL}).}

\item{categorical}{Character vector of categorical parameter names;
these are displayed as bar charts in the marginal row
(default \code{NULL}).}

\item{max_deviation}{Numeric scalar. Trials with
\eqn{|loss - mean(loss)|} above this threshold are excluded as outliers
(default \code{NULL}, no exclusion).}

\item{title}{Character scalar. Optional title displayed above the
combined figure (default \code{NULL}).}
}
\value{
Whatever the dispatched method returns (a \code{patchwork} figure
for \code{dicepro} objects).

A \code{patchwork} object which can be printed, saved with
\code{ggplot2::ggsave()}, or embedded in R Markdown documents.
}
\description{
Generic function for plotting the hyperparameter search report stored in a
\code{dicepro} object. Dispatches to \code{plot_hyperopt.dicepro}.

Builds a scatter-matrix of all evaluated \eqn{(\lambda, \gamma, p')}
configurations, color-coded by loss value, with violin/bar marginals for
the top 5\\% of trials. Reads all data from \code{x$trials} -- no file I/O.
}
\examples{
\donttest{
sim_data <- simulation(
  loi        = "gauss",
  scenario   = "hierarchical",
  nSample    = 5,
  nGenes     = 150,
  nCellsType = 10,
  sigma_bio  = 0.07,
  sigma_tech = 0.07,
  seed       = 2101
)

out <- dicepro(
  reference             = as.matrix(sim_data$W)[, -c(1, 5, 10)],
  bulk                  = as.matrix(sim_data$B),
  methodDeconv          = "FARDEEP",
  W_prime               = 0,
  bulkName              = "SimBulk",
  refName               = "SimRef",
  hp_max_evals          = 10,
  algo_select           = "random",
  output_path           = tempdir(),
  hspaceTechniqueChoose = "all",
  normalize             = FALSE
)
plot_hyperopt(out, params = c("lambda_", "gamma", "p_prime"))
}
}
\seealso{
\code{plot_hyperopt.dicepro}
}
